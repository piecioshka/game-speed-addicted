const settings = {
    map: {
        width: 800,
        height: 600
    },
    renderer: 'CANVAS'.toUpperCase(),
    container: '',
    defaultState: null,
    transparent: false,
    antialias: true,
    phisicsConfig: null
};

export default settings;