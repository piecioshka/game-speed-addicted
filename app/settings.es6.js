const settings = {
    map: {
        width: 1280,
        height: 800
    },
    renderer: 'canvas'.toUpperCase(),
    container: '',
    defaultState: null,
    transparent: false,
    antialias: true,
    physics: 'arcade'.toUpperCase(),
    game: {
        angularVelocity: 0,
        moveOffset: 15
    }
};

export default settings;